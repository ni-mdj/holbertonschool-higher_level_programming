-- Delete the database hbtn_0c_0 if it exists in MySQL server and do nothing if it does not exist.
-- This script deletes a database in MySQL server if it exists already and does nothing if it does not exist.

DROP DATABASE IF EXISTS hbtn_0c_0;
